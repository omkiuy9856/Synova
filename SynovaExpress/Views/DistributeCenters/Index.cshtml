@model IEnumerable<SynovaExpress.Models.DistributeCenter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Shipment.ShipmentNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Driver.DriverId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.UserId)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Shipment.ShipmentNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Driver.DriverId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.UserId)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
